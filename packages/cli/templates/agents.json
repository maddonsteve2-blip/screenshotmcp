{
  "$schema": "https://www.deepsyte.com/schemas/agents.schema.json",
  "name": "deepsyte",
  "version": "1.7.0",
  "description": "Browser screenshots, UX/SEO/accessibility audits, visual diffs, and pre-commit/CI gates as a CLI any AI agent framework (Claude Code, Cursor, Aider, Continue, OpenAI Agents) can shell out to.",
  "homepage": "https://www.deepsyte.com",
  "tools": [
    {
      "name": "screenshot",
      "description": "Capture a viewport-only screenshot of a URL. Returns a public CDN image URL.",
      "command": "deepsyte screenshot",
      "args": [
        { "name": "url", "type": "string", "required": true, "positional": true },
        { "name": "--width", "type": "integer", "default": 1280 },
        { "name": "--height", "type": "integer", "default": 800 },
        { "name": "--full-page", "type": "boolean" },
        { "name": "--delay", "type": "integer", "description": "Wait N ms after load" },
        { "name": "--format", "type": "string", "enum": ["png", "jpeg", "webp"], "default": "png" }
      ]
    },
    {
      "name": "fullpage",
      "description": "Capture the full scrollable page as a single tall image.",
      "command": "deepsyte fullpage",
      "args": [
        { "name": "url", "type": "string", "required": true, "positional": true },
        { "name": "--max-height", "type": "integer", "description": "Cap output image height in pixels" }
      ]
    },
    {
      "name": "review",
      "description": "Run an AI-powered UX review on a URL (accessibility / performance / SEO / content / mobile findings).",
      "command": "deepsyte review",
      "args": [{ "name": "url", "type": "string", "required": true, "positional": true }]
    },
    {
      "name": "seo",
      "description": "Extract SEO metadata (title, meta description, OG tags, headings, structured data, robots).",
      "command": "deepsyte seo",
      "args": [{ "name": "url", "type": "string", "required": true, "positional": true }]
    },
    {
      "name": "perf",
      "description": "Get Core Web Vitals + performance metrics (LCP, FCP, CLS, TTFB).",
      "command": "deepsyte perf",
      "args": [{ "name": "url", "type": "string", "required": true, "positional": true }]
    },
    {
      "name": "a11y",
      "description": "Get the accessibility tree for a URL.",
      "command": "deepsyte a11y",
      "args": [
        { "name": "url", "type": "string", "required": true, "positional": true },
        { "name": "--full", "type": "boolean", "description": "Include all nodes, not just interactive" }
      ]
    },
    {
      "name": "diff",
      "description": "Pixel-level visual diff between two URLs. Returns before/after/diff image URLs and a match score.",
      "command": "deepsyte diff",
      "args": [
        { "name": "urlA", "type": "string", "required": true, "positional": true },
        { "name": "urlB", "type": "string", "required": true, "positional": true },
        { "name": "--threshold", "type": "number", "default": 0.1 }
      ]
    },
    {
      "name": "check",
      "description": "CI gate. Audits URLs from .deepsyte/urls.json and exits non-zero when findings exceed the budget. Supports --report=github-comment for sticky PR comments.",
      "command": "deepsyte check",
      "args": [
        { "name": "--file", "type": "string", "default": ".deepsyte/urls.json" },
        { "name": "--max-findings", "type": "integer" },
        { "name": "--max-total", "type": "integer" },
        { "name": "--only", "type": "string", "description": "Comma-separated categories" },
        { "name": "--report", "type": "string", "enum": ["github-comment", "short", "json"] },
        { "name": "--report-out", "type": "string" }
      ]
    },
    {
      "name": "baseline-create",
      "description": "Capture and store a baseline screenshot for visual regression.",
      "command": "deepsyte baseline create",
      "args": [{ "name": "url", "type": "string", "required": true, "positional": true }]
    },
    {
      "name": "baseline-diff",
      "description": "Re-capture a URL and diff against its stored baseline.",
      "command": "deepsyte baseline diff",
      "args": [{ "name": "url", "type": "string", "required": true, "positional": true }]
    }
  ],
  "config": {
    "env": [
      { "name": "deepsyte_API_KEY", "required": true, "description": "API key from https://www.deepsyte.com/dashboard/keys" }
    ],
    "files": [
      { "path": ".deepsyte/urls.json", "description": "List of URLs the project cares about." },
      { "path": ".deepsyte/budget.json", "description": "Shared finding/perf thresholds." },
      { "path": ".deepsyte/baselines/*.json", "description": "Per-URL baseline references." }
    ]
  }
}
