# Visual-regression workflow. Drop into `.github/workflows/deepsyte-baselines.yml`
# to re-diff every baseline stored under .deepsyte/baselines/ on a schedule
# and on PRs that touch project URLs.
#
# Fails the job (and PR check) when any URL changes more than `--max-changed`
# percent of pixels vs its stored baseline.
name: deepsyte Visual Regression

on:
  pull_request:
    paths:
      - ".deepsyte/baselines/**"
      - ".deepsyte/urls.json"
      - ".github/workflows/deepsyte-baselines.yml"
  schedule:
    # Daily at 09:00 UTC — change to your release cadence.
    - cron: "0 9 * * *"
  workflow_dispatch: {}

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install deepsyte CLI
        run: npm install -g deepsyte
      - name: Verify baselines
        id: verify
        env:
          deepsyte_API_KEY: ${{ secrets.deepsyte_API_KEY }}
        run: |
          set +e
          deepsyte baseline verify \
            --threshold 0.1 \
            --max-changed 5 \
            --json > baselines-report.json
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: baselines-report
          path: baselines-report.json
      - name: Fail job on drift
        if: steps.verify.outputs.exit_code != '0'
        run: |
          echo "Baseline drift detected — see artifact 'baselines-report' for details."
          exit 1
