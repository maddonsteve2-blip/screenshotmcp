# Drop this into `.github/workflows/deepsyte.yml` to run UX/SEO audits
# on every pull request. Fails the build if thresholds are exceeded.
name: deepsyte Check

on:
  pull_request:
    paths:
      - ".deepsyte/**"
      - ".github/workflows/deepsyte.yml"
  workflow_dispatch: {}

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install deepsyte CLI
        run: npm install -g deepsyte
      - name: Run audit against project URL list
        id: audit
        env:
          deepsyte_API_KEY: ${{ secrets.deepsyte_API_KEY }}
        run: |
          set +e
          deepsyte check \
            --file .deepsyte/urls.json \
            --report github-comment \
            --report-out deepsyte-report.md
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
      - name: Post PR comment
        if: github.event_name == 'pull_request' && always()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          path: deepsyte-report.md
      - name: Fail job if audit failed
        if: steps.audit.outputs.exit_code != '0'
        run: exit 1
